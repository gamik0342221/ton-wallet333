# 💾 Как сохранить все файлы проекта

## 🎯 3 способа сохранить проект

---

## 🌟 Способ 1: Локальный архив (БЫСТРО)

### Автоматический скрипт:
```bash
bash backup-project.sh
```

Это создаст архив типа: `ton-wallet-backup-20251115-181109.tar.gz`

### Ручная команда:
```bash
tar -czf ton-wallet-backup.tar.gz \
  --exclude='node_modules' \
  --exclude='.git' \
  --exclude='dist' \
  --exclude='.astro' \
  .
```

### ✅ Преимущества:
- ⚡ Быстро (несколько секунд)
- 📦 Один файл со всем проектом
- 💾 Можно сохранить на флешку/диск

### 📥 Как восстановить:
```bash
# Распакуйте архив
tar -xzf ton-wallet-backup-20251115-181109.tar.gz

# Перейдите в папку
cd ton-wallet

# Установите зависимости
npm install

# Запустите
npm run dev
```

---

## 🚀 Способ 2: GitHub (РЕКОМЕНДУЕТСЯ)

### Если еще не настроили Git:
```bash
# 1. Инициализируйте Git
git init

# 2. Добавьте все файлы
git add .

# 3. Создайте первый коммит
git commit -m "Initial commit - TON Wallet project"

# 4. Создайте репозиторий на GitHub
# Перейдите на https://github.com/new

# 5. Подключите репозиторий
git remote add origin https://github.com/ВАШ_USERNAME/ton-wallet.git

# 6. Загрузите код
git push -u origin main
```

### Если Git уже настроен:
```bash
# Сохраните изменения
git add .
git commit -m "Update: добавлена мобильная оптимизация"
git push
```

### ✅ Преимущества:
- ☁️ Облачное хранение
- 📜 История всех изменений
- 🔄 Синхронизация между устройствами
- 👥 Можно делиться с другими
- 🔒 Бесплатно для приватных репозиториев

### 📥 Как восстановить:
```bash
# Склонируйте репозиторий
git clone https://github.com/ВАШ_USERNAME/ton-wallet.git

# Перейдите в папку
cd ton-wallet

# Установите зависимости
npm install

# Запустите
npm run dev
```

---

## ☁️ Способ 3: Облачное хранилище

### Google Drive / Dropbox / OneDrive:

1. Создайте локальный архив:
   ```bash
   bash backup-project.sh
   ```

2. Загрузите файл `ton-wallet-backup-*.tar.gz` в облако

3. Или скопируйте всю папку проекта в облачное хранилище

### ✅ Преимущества:
- ☁️ Автоматическая синхронизация
- 💾 Большой объем хранилища
- 🔒 Приватно
- 📱 Доступ с любого устройства

---

## 📦 Что исключается из архива

При создании архива **НЕ** сохраняются:
- ❌ `node_modules/` - зависимости (можно установить через `npm install`)
- ❌ `dist/` - собранные файлы (можно создать через `npm run build`)
- ❌ `.git/` - история Git (если используете GitHub)
- ❌ `.astro/` - кэш Astro
- ❌ `.wrangler/` - кэш Wrangler
- ❌ `*.log` - логи

### Почему?
Эти папки **огромные** (node_modules может быть 500+ MB), но их легко восстановить командой `npm install`.

---

## 🗂️ Структура архива

Ваш архив будет содержать:

```
ton-wallet-backup.tar.gz
├── src/                    # Исходный код
│   ├── components/         # React компоненты
│   ├── pages/             # Страницы Astro
│   ├── layouts/           # Layouts
│   ├── styles/            # Стили
│   └── lib/               # Утилиты
├── public/                # Статические файлы
├── generated/             # CSS от Webflow
├── package.json           # Зависимости
├── astro.config.mjs       # Конфигурация Astro
├── tsconfig.json          # Конфигурация TypeScript
├── .env                   # Переменные окружения
└── ... (и другие файлы)
```

---

## 🔄 Автоматическое резервное копирование

### Скрипт для автоматического бэкапа:

Файл уже создан: **`backup-project.sh`**

```bash
# Запустите скрипт
bash backup-project.sh

# Или добавьте в package.json
npm run backup
```

Скрипт создаст архив с датой и временем:
```
ton-wallet-backup-20251115-181109.tar.gz
```

---

## 💡 Советы

### 1. Регулярные бэкапы
Создавайте резервные копии:
- ✅ Перед большими изменениями
- ✅ После завершения новой функции
- ✅ Раз в неделю (минимум)

### 2. Используйте Git
GitHub - лучший способ для долгосрочного хранения:
- История всех изменений
- Можно откатиться к любой версии
- Облачное хранение бесплатно

### 3. Несколько копий
Правило 3-2-1:
- **3** копии ваших данных
- **2** разных носителя
- **1** копия в другом месте (облако)

### 4. Проверяйте бэкапы
Периодически проверяйте, что архив распаковывается:
```bash
# Создайте тестовую папку
mkdir test-restore
cd test-restore

# Распакуйте
tar -xzf ../ton-wallet-backup.tar.gz

# Проверьте
npm install
npm run dev
```

---

## 📋 Чеклист резервного копирования

Перед важными изменениями:
- [ ] Создать локальный архив (`bash backup-project.sh`)
- [ ] Закоммитить в Git (`git add . && git commit -m "..."`)
- [ ] Запушить на GitHub (`git push`)
- [ ] (Опционально) Загрузить в облако

---

## 🆘 Восстановление после сбоя

### Если потеряли файлы:

#### Из локального архива:
```bash
tar -xzf ton-wallet-backup-20251115-181109.tar.gz
cd ton-wallet
npm install
npm run dev
```

#### Из GitHub:
```bash
git clone https://github.com/ВАШ_USERNAME/ton-wallet.git
cd ton-wallet
npm install
npm run dev
```

#### Из облака:
1. Скачайте архив из Google Drive/Dropbox
2. Распакуйте (см. выше)
3. Установите зависимости

---

## 🛠️ Полезные команды

```bash
# Создать архив
bash backup-project.sh

# Посмотреть содержимое архива
tar -tzf ton-wallet-backup.tar.gz

# Распаковать архив
tar -xzf ton-wallet-backup.tar.gz

# Распаковать в определенную папку
tar -xzf ton-wallet-backup.tar.gz -C /path/to/folder

# Проверить размер архива
du -h ton-wallet-backup.tar.gz

# Найти все бэкапы
ls -lh *backup*.tar.gz

# Удалить старые бэкапы (старше 30 дней)
find . -name "*backup*.tar.gz" -mtime +30 -delete
```

---

## 📚 Дополнительная информация

### Git:
- [Git Setup Guide](GIT_SETUP.md)
- [GitHub Token Help](GITHUB_TOKEN_HELP.md)

### Документация:
- [Quick Start](QUICK_START.md)
- [Testing on Devices](TESTING_ON_DEVICES.md)
- [Mobile Optimization](MOBILE_OPTIMIZATION.md)

---

## ✅ Резюме

**Для быстрого бэкапа:**
```bash
bash backup-project.sh
```

**Для долгосрочного хранения:**
```bash
git add .
git commit -m "Update"
git push
```

**Для максимальной безопасности:**
- Используйте GitHub + локальный архив + облако

---

## 🎉 Готово!

Теперь ваш проект в безопасности! 💾

Рекомендуем настроить Git и GitHub для удобного долгосрочного хранения.

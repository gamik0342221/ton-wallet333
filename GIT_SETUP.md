# 🚀 Инструкция по сохранению проекта через Git/GitHub

## Шаг 1: Инициализация Git (если еще не сделано)

```bash
git init
git add .
git commit -m "Initial commit: TON Wallet project"
```

## Шаг 2: Создайте репозиторий на GitHub

1. Перейдите на https://github.com
2. Нажмите "New repository" (зеленая кнопка)
3. Введите название, например: `ton-wallet`
4. Выберите "Private" если хотите приватный репозиторий
5. НЕ добавляйте README, .gitignore или лицензию
6. Нажмите "Create repository"

## Шаг 3: Подключите локальный проект к GitHub

GitHub покажет команды, но вот основные:

```bash
# Добавьте удаленный репозиторий (замените YOUR_USERNAME и YOUR_REPO)
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO.git

# Или если используете SSH:
git remote add origin git@github.com:YOUR_USERNAME/YOUR_REPO.git

# Отправьте код на GitHub
git branch -M main
git push -u origin main
```

## Шаг 4: Клонирование на другой компьютер

Теперь вы можете скачать проект на любой ПК:

```bash
# Клонируйте репозиторий
git clone https://github.com/YOUR_USERNAME/YOUR_REPO.git

# Перейдите в папку
cd YOUR_REPO

# Установите зависимости
npm install

# Запустите проект
npm run dev
```

## ⚠️ ВАЖНО: Защита секретных данных

### Не загружайте .env файл на GitHub!

Убедитесь, что `.gitignore` содержит:

```
.env
.env.local
.env*.local
node_modules/
dist/
.wrangler/
```

### Как сохранить переменные окружения:

1. **Скопируйте .env отдельно** - сохраните в безопасном месте
2. **Создайте .env.example** - шаблон без секретных данных:

```bash
# .env.example
TELEGRAM_BOT_TOKEN=your_token_here
TELEGRAM_CHAT_ID=your_chat_id_here
WEBFLOW_CMS_SITE_API_TOKEN=your_token_here
WEBFLOW_API_HOST=your_host_here
```

3. **На новом ПК** - скопируйте .env.example в .env и вставьте реальные токены

## 📋 Полезные команды Git

```bash
# Проверить статус
git status

# Добавить изменения
git add .

# Сделать коммит
git commit -m "Описание изменений"

# Отправить на GitHub
git push

# Получить последние изменения
git pull

# Просмотреть историю
git log
```

## 🔄 Синхронизация между компьютерами

**На первом компьютере (после изменений):**
```bash
git add .
git commit -m "Updated design"
git push
```

**На втором компьютере:**
```bash
git pull
npm install  # если добавили новые пакеты
```

## 🆘 Решение проблем

### Если забыли какой remote:
```bash
git remote -v
```

### Если нужно изменить remote:
```bash
git remote set-url origin NEW_URL
```

### Если конфликты при pull:
```bash
git stash        # Сохранить локальные изменения
git pull         # Получить обновления
git stash pop    # Восстановить локальные изменения
```

## 💡 Советы

1. **Делайте коммиты часто** - легче отследить изменения
2. **Пишите понятные сообщения** - "fix button styling" лучше чем "update"
3. **Используйте ветки** для экспериментов:
   ```bash
   git checkout -b new-feature
   git checkout main  # вернуться на main
   ```
4. **Делайте backup .env** - храните отдельно от репозитория

---

🎉 Теперь ваш проект в безопасности и доступен с любого компьютера!

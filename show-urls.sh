#!/bin/bash

echo ""
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "🌐 Доступные URL для тестирования"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo ""

# Localhost
echo "💻 Локально (только на этом компьютере):"
echo "   http://localhost:3000"
echo ""

# Определяем операционную систему и получаем IP
if [[ "$OSTYPE" == "linux-gnu"* ]]; then
    IP=$(hostname -I | awk '{print $1}')
elif [[ "$OSTYPE" == "darwin"* ]]; then
    IP=$(ipconfig getifaddr en0 2>/dev/null || ifconfig | grep "inet " | grep -v 127.0.0.1 | awk '{print $2}' | head -n1)
elif [[ "$OSTYPE" == "msys" ]] || [[ "$OSTYPE" == "win32" ]]; then
    IP=$(ipconfig | grep "IPv4" | awk '{print $NF}' | head -n1)
fi

if [ -n "$IP" ]; then
    echo "📱 Локальная сеть (для телефона/планшета):"
    echo "   http://$IP:3000"
    echo ""
    echo "📋 QR код для этого URL:"
    echo "   https://api.qrserver.com/v1/create-qr-code/?size=300x300&data=http://$IP:3000"
    echo ""
else
    echo "⚠️  Не удалось определить IP-адрес"
    echo "   Используйте команду 'ipconfig' (Windows) или 'ifconfig' (Mac/Linux)"
    echo ""
fi

echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "📱 Инструкции:"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo ""
echo "1️⃣  Убедитесь, что сервер запущен:"
echo "    npm run dev"
echo ""
echo "2️⃣  Подключите телефон к той же Wi-Fi сети"
echo ""
if [ -n "$IP" ]; then
    echo "3️⃣  Откройте на телефоне: http://$IP:3000"
else
    echo "3️⃣  Найдите ваш IP и откройте: http://[ВАШ_IP]:3000"
fi
echo ""
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "☁️  Альтернатива: Публичный доступ"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo ""
echo "Cloudflare Tunnel (рекомендуется):"
echo "  1. npm install -g cloudflared"
echo "  2. cloudflared tunnel --url http://localhost:3000"
echo ""
echo "ngrok:"
echo "  1. Скачайте с https://ngrok.com/download"
echo "  2. ngrok http 3000"
echo ""
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo ""

# 🔐 Решение проблемы с вводом токена GitHub

## Проблема
При выполнении `git push` терминал просит пароль, но не видно что вводишь.

---

## ✅ САМОЕ ПРОСТОЕ РЕШЕНИЕ: Токен в URL

### Шаг 1: Получите ваши данные
- **GitHub Username:** ваш username (например: `alex123`)
- **Repository name:** название репо (например: `ton-wallet`)
- **Personal Access Token:** начинается с `ghp_` (например: `ghp_AbCdEf123456789`)

### Шаг 2: Выполните команды

```bash
# Удалите старый remote (если есть)
git remote remove origin

# Добавьте remote с токеном в URL
git remote add origin https://ВАШ_ТОКЕН@github.com/USERNAME/REPO.git

# Отправьте код
git branch -M main
git push -u origin main
```

### Пример с реальными данными:

Если ваши данные:
- Username: `alex123`
- Repo: `ton-wallet`
- Token: `ghp_AbCdEf123456789`

То команды будут:

```bash
git remote remove origin
git remote add origin https://ghp_AbCdEf123456789@github.com/alex123/ton-wallet.git
git branch -M main
git push -u origin main
```

---

## 🎯 Пошаговая инструкция

### 1. Создайте Personal Access Token на GitHub

1. Откройте: https://github.com/settings/tokens
2. Нажмите **"Generate new token"** → **"Generate new token (classic)"**
3. Заполните:
   - **Note:** "TON Wallet Push Access"
   - **Expiration:** 90 days (или больше)
   - **Scopes:** ✅ Отметьте **`repo`** (все подпункты)
4. Нажмите **"Generate token"**
5. **СКОПИРУЙТЕ ТОКЕН** (он начинается с `ghp_`)
   - ⚠️ Покажется только один раз!
   - Сохраните в безопасное место

### 2. Создайте репозиторий на GitHub

1. Откройте: https://github.com/new
2. Заполните:
   - **Repository name:** `ton-wallet`
   - **Private** или **Public** - на ваш выбор
   - ⚠️ **НЕ добавляйте** README, .gitignore
3. Нажмите **"Create repository"**
4. **Скопируйте URL репозитория** (он будет показан на экране)
   - Пример: `https://github.com/alex123/ton-wallet.git`

### 3. Выполните команды в терминале вашего проекта

```bash
# Проверьте, что вы в правильной папке
pwd

# Удалите старый remote если был
git remote remove origin

# Добавьте новый remote с токеном
# Формат: https://ТОКЕН@github.com/USERNAME/REPO.git
git remote add origin https://ghp_ВАШ_ТОКЕН_ЗДЕСЬ@github.com/ВАШ_USERNAME/ton-wallet.git

# Переименуйте ветку в main
git branch -M main

# Отправьте код на GitHub
git push -u origin main
```

### 4. Проверьте результат

Откройте ваш репозиторий на GitHub:
```
https://github.com/ВАШ_USERNAME/ton-wallet
```

Вы должны увидеть все файлы проекта! 🎉

---

## 🔧 Альтернативные методы

### Метод 2: Сохранить токен в системе

```bash
# Настройте Git для сохранения токена
git config --global credential.helper store

# Добавьте remote БЕЗ токена
git remote add origin https://github.com/USERNAME/REPO.git

# При первом push введите данные:
git push -u origin main
# Username: ваш_username
# Password: ваш_токен (не будет виден при вводе - это нормально!)

# Токен сохранится, больше вводить не нужно
```

### Метод 3: GitHub CLI (если установлен)

```bash
# Авторизуйтесь в GitHub CLI
gh auth login

# Создайте репозиторий и загрузите код одной командой
gh repo create ton-wallet --private --source=. --remote=origin --push
```

---

## ⚠️ Важные замечания

### Безопасность токена

1. **НЕ делитесь токеном** - это как пароль!
2. **НЕ коммитьте токен** в код
3. Если токен утек - удалите его на GitHub:
   - https://github.com/settings/tokens
   - Найдите токен и нажмите **Delete**
   - Создайте новый

### Скрыть токен из истории команд

После успешного push удалите токен из конфигурации:

```bash
# Посмотрите текущие remote
git remote -v

# Измените URL на версию БЕЗ токена
git remote set-url origin https://github.com/USERNAME/REPO.git

# Теперь токен не виден, но сохранен в credential.helper
```

---

## 🆘 Решение проблем

### "remote origin already exists"
```bash
git remote remove origin
# Попробуйте снова добавить
```

### "Authentication failed"
- Проверьте, что токен скопирован полностью
- Убедитесь, что токен начинается с `ghp_`
- Проверьте, что у токена есть права `repo`
- Токен мог истечь - создайте новый

### "Repository not found"
- Проверьте username - он чувствителен к регистру
- Проверьте название репозитория
- Убедитесь, что репозиторий создан на GitHub

### "Permission denied"
- Токен должен иметь права `repo`
- Проверьте, что вы владелец репозитория

---

## 📚 Дополнительные ресурсы

- Создание токена: https://github.com/settings/tokens
- Документация GitHub: https://docs.github.com/en/authentication
- Управление токенами: https://github.com/settings/tokens

---

## ✅ Чек-лист

- [ ] Создан Personal Access Token на GitHub
- [ ] Token имеет права `repo`
- [ ] Создан репозиторий на GitHub
- [ ] Скопирован полный URL репозитория
- [ ] Выполнена команда `git remote add origin` с токеном
- [ ] Выполнена команда `git push -u origin main`
- [ ] Код появился на GitHub

---

🎉 Успехов с загрузкой проекта на GitHub!

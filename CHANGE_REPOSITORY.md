# 🔄 Как переместить проект в новый репозиторий ton-viewer

## Вариант 1: Создать новый репозиторий и переключить remote (Быстро)

### Шаг 1: Создайте новый репозиторий на GitHub

1. **Откройте:** https://github.com/new
2. **Заполните:**
   ```
   Repository name: ton-viewer
   Description: TON Wallet Viewer
   
   ☑️ Public или Private (на ваш выбор)
   ❌ НЕ ставьте галочки на:
      - Add a README file
      - Add .gitignore
      - Choose a license
   ```
3. **Нажмите:** `Create repository`
4. **СКОПИРУЙТЕ URL:** `https://github.com/ваш-username/ton-viewer.git`

---

### Шаг 2: Переключите remote URL

Откройте терминал в папке вашего проекта:

```bash
# 1. Проверьте текущий remote
git remote -v

# Вывод будет:
# origin  https://github.com/старый-username/старый-repo.git (fetch)
# origin  https://github.com/старый-username/старый-repo.git (push)

# 2. Измените URL на новый репозиторий
# ЗАМЕНИТЕ на ваш URL!
git remote set-url origin https://github.com/ваш-username/ton-viewer.git

# 3. Проверьте что изменилось
git remote -v

# Теперь должно показать:
# origin  https://github.com/ваш-username/ton-viewer.git (fetch)
# origin  https://github.com/ваш-username/ton-viewer.git (push)

# 4. Отправьте весь код в новый репозиторий
git push -u origin main

# Если попросит авторизацию - введите username и Personal Access Token
```

**Готово!** Теперь ваш код в репозитории `ton-viewer` 🎉

---

### Шаг 3: Обновите Cloudflare Pages

Теперь нужно подключить Cloudflare к новому репозиторию:

#### Вариант A: Пересоздать проект (Рекомендуется)

1. **Откройте:** https://dash.cloudflare.com
2. **Перейдите:** Workers & Pages
3. **Нажмите на ваш проект** `ton-wallet33`
4. **Settings** → **Delete project** (внизу страницы)
   - Подтвердите удаление
   - ⚠️ Не волнуйтесь! Код остался на GitHub
5. **Создайте новый проект:**
   - Нажмите `Create application`
   - Выберите `Pages` → `Connect to Git`
   - Выберите репозиторий `ton-viewer`
   - Настройте так же как раньше:
     ```
     Project name: ton-viewer
     Framework: Astro
     Build command: npm run build
     Build output: dist
     ```
   - Добавьте переменные окружения:
     ```
     TELEGRAM_BOT_TOKEN = ваш_токен
     TELEGRAM_CHAT_ID = ваш_chat_id
     ```
   - Нажмите `Save and Deploy`

#### Вариант B: Переподключить репозиторий (Сложнее)

1. Откройте ваш проект в Cloudflare Pages
2. Settings → Builds & deployments → Configure Production deployments
3. Source → нажмите `Change source`
4. Выберите новый репозиторий `ton-viewer`
5. Сохраните

---

### Шаг 4: Обновите wrangler.jsonc

```bash
# Откройте файл wrangler.jsonc и измените name на:
"name": "ton-viewer"
```

Или выполните команду:

```bash
# Для Linux/Mac:
sed -i 's/"name": "ton-wallet33"/"name": "ton-viewer"/' wrangler.jsonc

# Для Windows (PowerShell):
(Get-Content wrangler.jsonc) -replace '"name": "ton-wallet33"', '"name": "ton-viewer"' | Set-Content wrangler.jsonc
```

Или просто вручную отредактируйте файл.

---

### Шаг 5: Закоммитьте изменение

```bash
git add wrangler.jsonc
git commit -m "Update project name to ton-viewer"
git push
```

---

## Вариант 2: Клонировать в новую папку (Если хотите начать с чистого листа)

### Шаг 1: Создайте новый репозиторий (как выше)

### Шаг 2: Клонируйте текущий проект в новую папку

```bash
# Перейдите в родительскую директорию
cd ..

# Клонируйте проект
git clone https://github.com/старый-username/старый-repo.git ton-viewer

# Перейдите в новую папку
cd ton-viewer

# Удалите старый remote
git remote remove origin

# Добавьте новый remote
git remote add origin https://github.com/ваш-username/ton-viewer.git

# Отправьте код
git push -u origin main
```

---

## Вариант 3: Переименовать существующий репозиторий на GitHub

Если хотите просто переименовать текущий репозиторий:

### Шаг 1: Переименуйте на GitHub

1. **Откройте ваш текущий репозиторий** на GitHub
2. **Нажмите:** Settings (вверху)
3. **В разделе "Repository name"** измените на: `ton-viewer`
4. **Нажмите:** `Rename`

⚠️ **ВАЖНО:** GitHub автоматически создаст редирект со старого имени!

### Шаг 2: Обновите local remote

```bash
# Git автоматически обновит URL при следующем push
# Но можно обновить вручную:

git remote set-url origin https://github.com/ваш-username/ton-viewer.git
```

### Шаг 3: Cloudflare автоматически подхватит изменения!

GitHub сохраняет редирект, поэтому Cloudflare продолжит работать без изменений! 🎉

---

## ✅ Проверка что всё работает

После любого из вариантов:

```bash
# 1. Проверьте remote
git remote -v

# Должно показать:
# origin  https://github.com/ваш-username/ton-viewer.git (fetch)
# origin  https://github.com/ваш-username/ton-viewer.git (push)

# 2. Попробуйте сделать изменение
echo "# TON Viewer" > TEST.md
git add TEST.md
git commit -m "Test commit"
git push

# 3. Проверьте что изменения появились на GitHub
```

---

## 🎯 Какой вариант выбрать?

| Вариант | Когда использовать | Сложность |
|---------|-------------------|-----------|
| **Вариант 1** | Хотите переместить код в новый репозиторий | ⭐⭐ |
| **Вариант 2** | Хотите сохранить старый и создать новый | ⭐⭐⭐ |
| **Вариант 3** | Просто переименовать текущий репозиторий | ⭐ (Самый простой!) |

### 💡 Моя рекомендация:

**Используйте Вариант 3** - просто переименуйте существующий репозиторий на GitHub!

Это:
- ✅ Самое простое
- ✅ Сохраняет всю историю коммитов
- ✅ Cloudflare продолжит работать автоматически
- ✅ Не нужно пересоздавать проект в Cloudflare

---

## 📝 Краткая инструкция (Вариант 3 - Рекомендуется)

```bash
# 1. Переименуйте репозиторий на GitHub
#    → Settings → Repository name → "ton-viewer" → Rename

# 2. Обновите local remote (опционально)
git remote set-url origin https://github.com/ваш-username/ton-viewer.git

# 3. Обновите wrangler.jsonc
#    Измените "name": "ton-viewer"

# 4. Закоммитьте
git add wrangler.jsonc
git commit -m "Update project name"
git push

# ГОТОВО! 🎉
```

---

## 🆘 Возможные проблемы

### Проблема: "Permission denied" при push

**Решение:** Создайте новый Personal Access Token:
1. https://github.com/settings/tokens
2. Generate new token → Generate new token (classic)
3. Выберите `repo` scope
4. Сгенерируйте и скопируйте
5. Используйте вместо пароля при push

### Проблема: Cloudflare не видит новый репозиторий

**Решение:**
1. https://github.com/settings/installations
2. Найдите "Cloudflare Pages"
3. Configure → дайте доступ к `ton-viewer`

### Проблема: История коммитов не сохранилась

**Это нормально** если вы создали новый репозиторий. Если хотите сохранить историю - используйте Вариант 3 (переименование).

---

## 🎉 Готово!

Теперь ваш проект в репозитории `ton-viewer`!

**Новый URL репозитория:**
```
https://github.com/ваш-username/ton-viewer
```

**URL сайта останется прежним** (или можете изменить в настройках Cloudflare):
```
https://ton-wallet33.pages.dev
→ можно изменить на ton-viewer.pages.dev
```

Удачи! 🚀

# 💻 Как скачать проект на свой компьютер

## 🎯 У вас есть 3 способа:

---

## 🌟 Способ 1: Скачать архив (САМЫЙ ПРОСТОЙ)

### ✅ Преимущества:
- ⚡ Быстро (1 файл)
- 📦 Готов к использованию
- 💾 Можно сохранить на флешку

### 📖 Пошаговая инструкция:

#### Шаг 1: Создайте архив
```bash
npm run backup
```

Или:
```bash
bash backup-project.sh
```

**Результат:** Файл `ton-wallet-cloud-backup-20251115-181741.tar.gz` (98 MB)

#### Шаг 2: Скачайте файл на свой ПК

**Если работаете в Webflow/браузерной среде:**

1. **Найдите файл в файловом менеджере**
   - Откройте панель файлов в вашей среде разработки
   - Найдите файл `ton-wallet-cloud-backup-*.tar.gz`
   - Нажмите правой кнопкой → **"Download"** или **"Скачать"**

2. **Или используйте терминал для создания ссылки:**
   ```bash
   # Покажет путь к файлу
   pwd
   ls -lh ton-wallet-cloud-backup-*.tar.gz
   ```

#### Шаг 3: Распакуйте на своём ПК

**Windows:**
```cmd
# Используйте 7-Zip или WinRAR
# Или через командную строку (если есть tar):
tar -xzf ton-wallet-cloud-backup-20251115-181741.tar.gz
cd ton-wallet
npm install
npm run dev
```

**macOS / Linux:**
```bash
tar -xzf ton-wallet-cloud-backup-20251115-181741.tar.gz
cd ton-wallet
npm install
npm run dev
```

---

## 🚀 Способ 2: Через GitHub (РЕКОМЕНДУЕТСЯ)

### ✅ Преимущества:
- ☁️ Удобно для долгосрочной работы
- 🔄 Легко обновлять
- 📜 История всех изменений
- 👥 Можно работать с разных компьютеров

### 📖 Пошаговая инструкция:

#### Шаг 1: Создайте репозиторий на GitHub

1. Откройте https://github.com/new
2. Назовите репозиторий: **"ton-wallet"**
3. Выберите **"Private"** (приватный)
4. НЕ добавляйте README, .gitignore или лицензию
5. Нажмите **"Create repository"**

#### Шаг 2: Загрузите код на GitHub (в вашей среде разработки)

```bash
# Инициализируйте Git (если ещё не сделали)
git init

# Добавьте все файлы
git add .

# Создайте первый коммит
git commit -m "Initial commit - TON Wallet project"

# Добавьте удалённый репозиторий
git remote add origin https://github.com/ВАШ_USERNAME/ton-wallet.git

# Загрузите код
git branch -M main
git push -u origin main
```

**Примечание:** Замените `ВАШ_USERNAME` на ваш логин GitHub

**Если просит токен:**
См. инструкцию в [GITHUB_TOKEN_HELP.md](GITHUB_TOKEN_HELP.md)

#### Шаг 3: Скачайте на свой ПК

Теперь на **вашем компьютере** выполните:

```bash
# Склонируйте репозиторий
git clone https://github.com/ВАШ_USERNAME/ton-wallet.git

# Перейдите в папку
cd ton-wallet

# Установите зависимости
npm install

# Запустите проект
npm run dev
```

#### Шаг 4: Работайте на своём ПК

```bash
# Редактируйте файлы...
# Сохраните изменения в Git:

git add .
git commit -m "Описание изменений"
git push

# Обновите код на другом компьютере:
git pull
```

---

## 📂 Способ 3: Прямое копирование файлов

### Если у вас есть прямой доступ к файловой системе:

#### Windows:

1. **Через проводник:**
   - Откройте папку проекта
   - Выделите все файлы (Ctrl + A)
   - Скопируйте (Ctrl + C)
   - Вставьте на своём ПК в нужную папку (Ctrl + V)

2. **Через командную строку:**
   ```cmd
   xcopy C:\path\to\project C:\Users\YourName\ton-wallet /E /I
   cd C:\Users\YourName\ton-wallet
   npm install
   npm run dev
   ```

#### macOS / Linux:

```bash
# Скопируйте всю папку
cp -r /path/to/project ~/ton-wallet

# Перейдите в папку
cd ~/ton-wallet

# Установите зависимости
npm install

# Запустите
npm run dev
```

---

## 🖥️ Способ 4: Через SCP/SFTP (если есть SSH доступ)

### Если у вас есть SSH доступ к серверу:

```bash
# На вашем ПК выполните:
scp -r user@server:/path/to/project ~/ton-wallet

# Или используйте SFTP клиент:
# - FileZilla (Windows/Mac/Linux)
# - WinSCP (Windows)
# - Cyberduck (Mac)

# Подключитесь к серверу
# Скачайте всю папку проекта
```

---

## 📋 Что нужно установить на вашем ПК?

### Обязательно:

1. **Node.js** (v18 или выше)
   - Windows: https://nodejs.org/en/download/
   - macOS: `brew install node` или с сайта
   - Linux: `sudo apt install nodejs npm`

2. **Git** (для способа 2)
   - Windows: https://git-scm.com/download/win
   - macOS: `brew install git` или уже установлен
   - Linux: `sudo apt install git`

### Опционально:

3. **Visual Studio Code** (редактор кода)
   - https://code.visualstudio.com/

4. **7-Zip** (для распаковки .tar.gz на Windows)
   - https://www.7-zip.org/

---

## 🎯 Рекомендуемый порядок действий

### Для новичков:

1. ✅ Создайте архив: `npm run backup`
2. ✅ Скачайте файл на ПК
3. ✅ Распакуйте
4. ✅ Установите Node.js (если нет)
5. ✅ Выполните: `npm install`
6. ✅ Запустите: `npm run dev`

### Для опытных:

1. ✅ Создайте репозиторий на GitHub
2. ✅ Загрузите код: `git push`
3. ✅ Склонируйте на ПК: `git clone`
4. ✅ Установите зависимости: `npm install`
5. ✅ Запустите: `npm run dev`

---

## 🛠️ После скачивания на ПК

### Первый запуск:

```bash
# 1. Перейдите в папку проекта
cd ton-wallet

# 2. Установите зависимости (это займёт 2-3 минуты)
npm install

# 3. Создайте файл .env (если нужен)
# Скопируйте .env.example в .env
cp .env.example .env

# 4. Запустите проект
npm run dev
```

### Проект запустится на:
```
http://localhost:4321
```

Откройте браузер и перейдите по этому адресу! 🎉

---

## 📊 Структура проекта

После скачивания у вас будет:

```
ton-wallet/
├── src/                      # Исходный код
│   ├── components/          # React компоненты
│   │   ├── WalletSplash.tsx
│   │   ├── SeedImportModal.tsx
│   │   └── ui/              # UI компоненты (shadcn)
│   ├── pages/               # Страницы Astro
│   │   ├── index.astro
│   │   └── api/             # API роуты
│   ├── layouts/             # Layouts
│   │   └── main.astro
│   ├── styles/              # Стили
│   │   └── global.css
│   └── lib/                 # Утилиты
│       └── base-url.ts
├── public/                   # Статические файлы
│   ├── favicon.ico
│   └── ton-logo.svg
├── generated/                # CSS от Webflow
│   ├── fonts.css
│   └── webflow.css
├── package.json              # Зависимости
├── astro.config.mjs          # Конфигурация Astro
├── tsconfig.json             # Конфигурация TypeScript
├── .env.example              # Пример переменных окружения
└── README.md                 # Документация

НЕ будет (это нормально):
├── node_modules/             # Создастся при npm install
└── dist/                     # Создастся при npm run build
```

---

## 🔧 Полезные команды

После скачивания проекта:

```bash
# Разработка (запуск локального сервера)
npm run dev

# Сборка для продакшена
npm run build

# Предпросмотр собранного проекта
npm run preview

# Создать бэкап
npm run backup

# Проверка типов TypeScript
npm run astro check
```

---

## ❗ Возможные проблемы и решения

### Проблема: "npm: command not found"
**Решение:** Установите Node.js
- Windows: https://nodejs.org/en/download/
- macOS: `brew install node`
- Linux: `sudo apt install nodejs npm`

### Проблема: "Cannot find module"
**Решение:** Установите зависимости
```bash
npm install
```

### Проблема: Порт 4321 занят
**Решение:** Используйте другой порт
```bash
npm run dev -- --port 3000
```

### Проблема: Ошибки при npm install
**Решение:** Очистите кэш и попробуйте снова
```bash
npm cache clean --force
rm -rf node_modules package-lock.json
npm install
```

### Проблема: Не распаковывается .tar.gz на Windows
**Решение:** Установите 7-Zip
- https://www.7-zip.org/
- Или используйте Git Bash (идёт с Git)

---

## 🎓 Обучающие материалы

После того как скачаете проект, рекомендую изучить:

1. **Astro документация**
   - https://docs.astro.build/

2. **React документация**
   - https://react.dev/

3. **Tailwind CSS**
   - https://tailwindcss.com/docs

4. **Git для начинающих**
   - https://git-scm.com/book/en/v2

---

## 📖 Дополнительные инструкции

- 📄 **BACKUP_GUIDE.md** - как делать резервные копии
- 📄 **GIT_SETUP.md** - настройка Git и GitHub
- 📄 **GITHUB_TOKEN_HELP.md** - помощь с токенами GitHub
- 📄 **CLOUD_STORAGE_GUIDE.md** - сохранение в облако
- 📄 **QUICK_START.md** - быстрый старт
- 📄 **TESTING_ON_DEVICES.md** - тестирование на устройствах
- 📄 **MOBILE_OPTIMIZATION.md** - мобильная оптимизация
- 📄 **DEPLOYMENT_GUIDE.md** - деплой на сервер

---

## ✅ Чеклист

Перед началом работы на своём ПК:

- [ ] Node.js установлен (проверьте: `node --version`)
- [ ] Git установлен (проверьте: `git --version`)
- [ ] Проект скачан на ПК
- [ ] Зависимости установлены (`npm install`)
- [ ] Проект запускается (`npm run dev`)
- [ ] Браузер открыл http://localhost:4321
- [ ] Всё работает! 🎉

---

## 🎉 Готово!

Теперь вы можете работать с проектом на своём компьютере! 💪

**Следующие шаги:**
1. Откройте проект в VS Code: `code .`
2. Изучите структуру файлов
3. Попробуйте изменить что-нибудь
4. Сохраняйте изменения в Git

**Нужна помощь?**
- Читайте README.md в корне проекта
- Смотрите другие .md файлы с инструкциями
- Задавайте вопросы! 🙂

---

## 🆘 Экстренная помощь

Если что-то пошло не так:

1. **Проверьте версии:**
   ```bash
   node --version  # должна быть v18 или выше
   npm --version   # должна быть v9 или выше
   ```

2. **Переустановите зависимости:**
   ```bash
   rm -rf node_modules package-lock.json
   npm install
   ```

3. **Создайте новый бэкап:**
   ```bash
   npm run backup
   ```

4. **Попробуйте скачать заново**

---

Удачи! 🚀
